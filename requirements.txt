# ─────────────────────────────────────────────
# Dracarys — full dependency list
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# Django backend
Django==6.0.5
djangorestframework==3.17.1
django-cors-headers==4.9.0
asgiref==3.11.1
gunicorn==26.0.0
sqlparse==0.5.5

# PDF extraction (primary: PyMuPDF, OCR fallback: pytesseract)
pymupdf>=1.24.0
pytesseract>=0.3.13
# Optional: pdf2image — only needed if documents are scanned/image-only PDFs
# pip install pdf2image  (also requires Poppler on Windows)

# DOCX extraction
python-docx>=1.1.0

# Chunking
langchain-text-splitters>=0.3.0

# Embeddings — all-MiniLM-L6-v2
sentence-transformers>=3.0.0

# Vector database
chromadb>=0.5.0

# LLM (Groq)
groq>=0.9.0

# Utilities
python-dotenv>=1.0.0
packaging>=21.3
